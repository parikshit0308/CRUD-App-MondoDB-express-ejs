<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        
            <a class="text-blue-500" href="/">Back To Home</a>

            <h2 class="text-3xl tracking-tighter mb-5 mt-3">All Users</h2>
            <div class="users flex gap-3 flex-wrap mt-4 ">

                <% if(users.length > 0){ %>

                    <%  users.forEach(function(user){ %>
                        <div class="user w-56 p-3 bg-zinc-700 rounded overflow-hidden">
                            <div class="w-full h-48 bg-zinc-500">
                                <img class="w-full h-full object-cover" src=" <%= user.image %> " alt="">
                            </div>
                            <h3 class="text-base mt-1 tracking-tighter"> <%= user.name %> </h3>
                            <h3 class="text-xs text-zinc-400 tracking-tighter"> <%= user.email %> </h3>
                            <div class="flex gap-3 mt-2 ">
                                <a class="text-zinc-200 text-sm" href="">Edit User</a>
                                <a class="text-red-500 text-sm" href="/delete/<%= user._id %>">Delete User</a>
                            </div>
                        </div>
                    <%  }) %>  

                <% }else{ %>

                    <h3 class="text-3xl text-zinc-400">No Users Found.</h3>

                <% } %>

  
                
            </div>
    </div>

</body>
</html>